{
  "name": "kimono-one-image-processor",
  "version": "1.0.0",
  "description": "AWS Lambda function for image processing",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "build": "npm install && zip -r function.zip .",
    "deploy": "aws lambda update-function-code --function-name kimono-one-image-processor --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "sharp": "^0.33.0"
  }
}
