# ==========================================
# 数据库配置
# ==========================================

# 🔴 生产环境 (Vercel) - 必须使用 Supabase Connection Pooler
# 格式: postgresql://postgres.[PROJECT_REF]:密码@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true
# 关键配置:
#   - 端口必须是 6543 (不是 5432)
#   - 主机名包含 .pooler.
#   - 必须添加 &pgbouncer=true 参数
#   - 密码中的特殊字符需要 URL 编码 (^ = %5E, $ = %24, @ = %40)
# 示例: DATABASE_URL="postgresql://postgres.abcdefgh:pass%24word@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true"

# 🟢 本地开发环境 - 可以使用直接连接
# 格式: postgresql://postgres:密码@db.xxxxx.supabase.co:5432/postgres?sslmode=require
# 本地开发不需要 pgbouncer=true 参数
# 示例: DATABASE_URL="postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres?sslmode=require"

DATABASE_URL="postgresql://postgres:your-password@db.xxxxxx.supabase.co:5432/postgres?sslmode=require"

# NextAuth 认证配置
# 生成方式：openssl rand -base64 32
AUTH_SECRET="your-random-secret-key-here"

# 应用 URL
# 开发环境：http://localhost:3000
# 生产环境：https://your-domain.vercel.app
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# 环境
# 可选值：development | production | test
NODE_ENV="development"

# ===== 可选配置 =====

# Google OAuth（用于 Google 登录）
# 在 Google Cloud Console 创建 OAuth 2.0 凭据
# https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID="your-google-client-id"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"

# 邮件服务（如果需要发送邮件）
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"
# SMTP_FROM="noreply@hefumiyabi.com"

# 文件上传（如果使用 Cloudinary）
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your-cloud-name"
# CLOUDINARY_API_KEY="your-api-key"
# CLOUDINARY_API_SECRET="your-api-secret"

# 支付网关（如果集成支付）
# STRIPE_PUBLIC_KEY="pk_test_..."
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# Google Analytics（可选）
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"
